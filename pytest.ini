[pytest]
markers =
    smoke:      Fast sanity check — scope + DMM readable
    regression: Full parametrized tests — freq + voltage
    slow:       Stability / multi-sample tests

addopts = -v --tb=short
